# 更新日志

本文件记录 Xinyue AI 的重要发布变化。当前发布版本是 1.0.1；「未发布」中的条目尚未单独打版本号。

## 未发布

### 2026-09-24

- 启动时幂等补齐内置能力：12 个助手、8 个工具、7 个技能分类、14 个预装技能。预装技能无需安装即可使用，用户可停用；管理端可恢复默认技能。新建知识库提供 6 个模板。
- 修复图片渠道切换后残留上一渠道结果、聊天流式输出丢字、图片/视频任务并发补建重复会话、画布新节点被顶栏挡住，以及豆包首页「对话」按钮无响应。
- 临时聊天中的图片和视频不再进入历史记录；发布作品时不再把未填写的公开提示词自动替换成原图提示词。
- 用户自带密钥的渠道下载媒体时始终校验内网地址；只有后台配置的渠道在设置了代理时才走代理。外部技能市场在代码中默认关闭，示例配置保持开启。
- 删除已不再使用的站点皮肤、用户级侧栏和个人 Sub2API 字段，并附带迁移。本地开发入口统一为用户端 5173、管理端 5174、API 3100。
- 清理未使用的本地文件与过期文档，重写 README、架构、开发、部署、设计系统和开源参考文档。
- 统一 GitHub 文档用语：安全策略、更新日志、第三方声明和贡献流程改为一致的中文表述；部署与开发命令示例改为 bash。数据库角色 `flux` 与库名 `flux_studio` 仍是内部标识，连接串未改。
- 验证：UI 操作审计通过，单元测试 379 项，Playwright 45 项，用户端、管理端与服务端构建通过，Prisma 130 项迁移已全部应用。

### 修复记录（2026-09-07）

- 修复后台渠道编辑提交 API 密钥时因 Prisma 更新参数使用不存在的 `templateId` 和 `lastRotatedAt` 字段而返回 `Internal server error` 的问题；渠道模板改用关系 `connect/disconnect` 更新，并同步修正新增渠道路径。
- 修复 NewAPI 渠道默认关闭原生联网导致用户开启“联网”后仍无法搜索的问题；NewAPI 默认使用 OpenAI 兼容原生搜索，并迁移现有仍使用默认关闭值的渠道。
- 修复模型选择器在视频模型为空时隐藏全部能力分类的问题；现在仍显示“视频模型”，并明确提示“没有可用的视频模型”。
- 修复已启用但尚未绑定可用渠道的模型被前端目录完全过滤的问题；模型会显示为“未配置”，但不会被当成可提交模型。
- 修复管理员修改上游渠道 Base URL 时误清空原有 API Key 的问题；留空保留、填写新 Key 更新、明确提交空值才清除。
- 修改管理员渠道 API Key、Base URL 或用户 BYOK 配置后，重置旧健康状态、失败计数和冷却状态，避免新配置继续沿用旧故障状态。
- 修复管理端模型页把“已启用”误显示为“前端可见”的问题；现在会区分前端可见、渠道待检测、未配置渠道和已停用。
- 修复前端已选模型没有可用渠道时仍笼统提示“暂无可用模型”的问题；现在区分“未绑定渠道”“未配置渠道 API key”“渠道待检测”和“渠道冷却中”。
- 修复工作区认证状态晚于页面挂载时模型目录不重新切换到用户接口的问题；登录态恢复后会自动刷新 `/users/me/models`，避免前端继续使用旧的公共目录。
- 修复原 6000 预览入口绕过 Nginx 同源 `/v1` 代理、误请求宿主机内部 3100 导致模型目录为空的问题。当时把浏览器入口改到普通 Chrome 可访问的 6001；2026-09-24 起，本地开发入口改为用户端 5173、管理端 5174、API 3100。
- 增加管理员渠道、用户凭据、未配置模型和模型默认选择的回归测试。
- 当时的本地浏览器预览和测试入口是 `http://127.0.0.1:6001`。Docker 内部服务端口只在 Compose 网络内使用，不对宿主机暴露。该预览端口已由 2026-09-24 的本地入口调整取代。

## 1.0.1

首个公开版本后的 Release Candidate 稳定性更新。

### 新增

- 用户工作区、多模型对话、流式响应、会话历史与文件能力。
- 无限画布、图片、视频、商品视觉和异步生成任务。
- 用户、权限、Provider、模型、价格、额度、订单和系统配置管理后台。
- OpenAI Compatible、Anthropic、Gemini 与本地 Worker 接入能力。
- PostgreSQL、Redis、BullMQ、Docker Compose 和首次启动初始化向导。

### 稳定性与账务

- 增加额度 Reservation 生命周期、幂等结算和 Ledger 一致性保护。
- 增加 ProviderAttempt 审计、模型故障切换和 Worker lease fencing。
- 统一 Chat、Image、Video 等生成任务的失败释放与恢复路径。
- 增加健康检查、就绪检查和启动配置校验。

### 安全

- 管理端 RBAC 改为 fail-closed，并保留 SUPER_ADMIN 完整权限。
- 首次初始化使用一次性安装令牌，不提供固定管理员密码。
- 外部 HTTP 请求增加协议、私网地址和重定向逐跳校验。
- 生产启动拒绝空值、示例值和低强度关键配置。
- 默认关闭未经审查的外部 Prompt 与技能市场同步。

### 验证

- 210 项单元测试、44 项 Playwright E2E 基线及 UI action audit 通过。
- 用户端、管理端、服务端构建通过。
- 116 项 Prisma migration 在全新本地 PostgreSQL 中部署通过。

## 1.0.0

首次公开版本。

- 提供用户工作区、管理后台、统一 API、Provider 路由和生成任务。
- 提供 Token 额度、创作点、账单流水和会员配置能力。
- 提供 PostgreSQL、Redis、BullMQ、Docker Compose 和一键部署支持。
